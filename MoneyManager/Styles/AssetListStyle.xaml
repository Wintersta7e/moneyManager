<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:MoneyManager.Styles">

    <Style TargetType="TextBlock" x:Key="ListTitle">
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Margin" Value="30, 0, 0, 0"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="ListValue" BasedOn="{StaticResource ListTitle}">
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Margin" Value="0, 0, 30, 0"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="ListDate" BasedOn="{StaticResource ListTitle}">
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Margin" Value="0, 0, 60, 0"/>
    </Style>

    <Style TargetType="ListViewItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="0, 5, 0, 5"/>
    </Style>

</ResourceDictionary>